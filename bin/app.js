// Generated by CoffeeScript 1.6.3
(function() {
  var SIFMImg, SIFMText, app, express, port;

  express = require('express');

  SIFMText = require('./SIFMText.js');

  SIFMImg = require('./SIFMImg.js');

  app = express();

  app.get('/ipsum', function(req, res) {
    var sifm;
    sifm = new SIFMText({
      count: 10,
      units: "paragraphs",
      format: "html"
    });
    return res.send(sifm.ipsum());
  });

  app.get('/bacon', function(req, res) {
    var sifm;
    sifm = new SIFMText({
      count: 10,
      units: "paragraphs",
      format: "html"
    });
    return res.send(sifm.bacon());
  });

  app.get('/cats', function(req, res) {
    var sifm;
    sifm = new SIFMImg();
    return sifm.cat(function(img) {
      return res.sendfile(img);
    });
  });

  app.get('/cats/bw', function(req, res) {
    var sifm;
    sifm = new SIFMImg();
    return sifm.catbw(function(img) {
      return res.sendfile(img);
    });
  });

  app.get('/cats/:width/:height', function(req, res) {
    var sifm;
    sifm = new SIFMImg();
    return sifm.catsize(req.params.width, req.params.height, function(img) {
      return res.sendfile(img);
    });
  });

  app.get('/cats/:width/:height/bw', function(req, res) {
    var sifm;
    sifm = new SIFMImg();
    return sifm.catsizebw(req.params.width, req.params.height, function(img) {
      return res.sendfile(img);
    });
  });

  port = process.env.PORT || 3000;

  app.listen(port, function() {
    return console.log('Listening on ' + port);
  });

}).call(this);
